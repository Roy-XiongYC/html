<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:tx="http://www.springframework.org/schema/tx" xmlns:aop="http://www.springframework.org/schema/aop"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
	http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.1.xsd
	http://www.springframework.org/schema/tx  http://www.springframework.org/schema/tx/spring-tx-4.1.xsd
    http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.1.xsd
">

	<!-- redis 常量 配置 -->
	<!-- 引入属性文件 -->
	<context:property-placeholder location="classpath:redis.properties" ignore-unresolvable="true"/>
	
	<bean id="redisConfig" class="com.person.framework.redis.JedisPools" init-method="init">
		<property name="serviceName" value="${REDIS.SERVICE_NAME}" />
		<property name="serviceUrl" value="${REDIS.SERVICE_URL}" />
		<property name="redisPort" value="${REDIS.REDIS_PORT}" />
		<property name="maxIdle" value="${REDIS.MAX_IDLE}" />
		<property name="maxTotal" value="${REDIS.MAX_TOTAL}" />
		<property name="maxWaitMillis" value="${REDIS.MAX_WAIT_MILLIS}" />
		<property name="testOnBorrow" value="${REDIS.TEST_ON_BORROW}" />
		<property name="testOnReturn" value="${REDIS.TEST_ON_RETURN}" />
		<property name="timeout" value="${REDIS.TIMEOUT}" />
		<property name="auth" value="${REDIS.AUTH}" />
	</bean>
</beans>